meta {
  name: Update Alert Rule
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/indexes/{{indexId}}/alerts/{{alertRuleId}}
  body: json
  auth: none
}

body:json {
  {
    "enabled": false
  }
}
