meta {
  name: Create Saved Query
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/indexes/{{indexId}}/saved-queries
  body: json
  auth: none
}

body:json {
  {
    "name": "Errors last 24h",
    "description": "Quick triage filter",
    "query": "level:error"
  }
}
