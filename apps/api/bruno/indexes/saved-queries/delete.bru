meta {
  name: Delete Saved Query
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/indexes/{{indexId}}/saved-queries/{{savedQueryId}}
  body: none
  auth: none
}

vars:pre-request {
  savedQueryId: 13
}
