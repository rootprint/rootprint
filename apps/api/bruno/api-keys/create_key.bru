meta {
  name: Create Ingest Key
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/api-keys
  body: json
  auth: none
}

body:json {
  {
    "name": "prod-shipper",
    "indexId": "{{indexId}}"
  }
}
