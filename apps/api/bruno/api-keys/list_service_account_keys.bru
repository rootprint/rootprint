meta {
  name: List Service Account Keys
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/api-keys/service-account
  body: none
  auth: none
}

docs {
  Admin oversight of service-account API keys (read-only keys minted for a
  service-account user). Personal keys are self-service only — see personal-keys.
}
