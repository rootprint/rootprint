meta {
  name: Create Invite
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/invites
  body: json
  auth: none
}

body:json {
  {
    "name": "New User",
    "email": "user@example.com",
    "role": "user"
  }
}
