meta {
  name: Invite User
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/users
  body: json
  auth: none
}

body:json {
  {
    "name": "New User",
    "email": "user@example.com",
    "role": "user"
  }
}
