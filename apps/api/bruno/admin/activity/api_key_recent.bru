meta {
  name: API Key Recent Searches
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/admin/activity/api-keys/{{apiKeyId}}/recent
  body: none
  auth: none
}

params:query {
  ~window: 24h
  ~offset: 0
  ~limit: 100
  ~status: any
}
