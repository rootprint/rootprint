// UI-local types only. Prefer `import type { ... } from 'api/types'` for anything
// that has a backend counterpart. This file stays small on purpose.

// oxlint-disable-next-line unicorn/require-module-specifiers
export {};
